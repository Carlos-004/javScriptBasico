//Como acceder a los valores de un objecto
const producto = {
    nombre:"Monitor",
    precio:3000,
    id:0,
    disponible:true,
}


console.log(`El ${producto.nombre} tiene un valor de $${producto.precio} USD`);//El Monitor tiene un valor de $3000 USD